info:
  name: buscar por id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/albuns/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
