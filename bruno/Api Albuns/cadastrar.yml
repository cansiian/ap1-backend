info:
  name: cadastrar
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/api/albuns"
  body:
    type: json
    data: |-
      {
        "titulo": "A Moon Shaped Pool",
        "anoLancamento": 2016,
        "numeroFaixas": 11
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
