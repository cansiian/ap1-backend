info:
  name: confirmar remoção
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/albuns/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
