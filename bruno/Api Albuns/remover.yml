info:
  name: remover
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/albuns/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
